/* -------------------------------------------------------------------------- */
/*                                   jQuery                                   */
/* -------------------------------------------------------------------------- */

/* --------------------------- Methode JavaScript --------------------------- */

// function name(params) {

// }

// var maFct = function name(params) {

// }
// maFct();

var date = new Date();
var yearNow = date.getFullYear();
var monObj = {
    name: "Anissa",
    city: "Mons",
    year: 1979,
    calculateAge: function () {
        var age = yearNow - this.year;
        console.log(age);
    }

}
monObj.calculateAge();

/* -------------------------------------------------------------------------- */


/* ---------------------------------- IIFE ---------------------------------- */
/* -------------- Expression de fonction invoquée immédiatement ------------- */

//function () { }

(function (msg) {
    console.log(msg);
})("coucou");

/* -------------------------- Lancer le JQuery ------------------------- */

(function ($) {

    /* ----------------------------- Document Ready ----------------------------- */

    /* --------------------------- Version Vanilla JS --------------------------- */
    //document.addEventListener('DOMContentLoaded', function () { }) 
    /* -------------------------------------------------------------------------- */


    /* ----------------------------- Version JQuery ----------------------------- */

    $(document).ready(function () {

        /* -------------------------------------------------------------------------- */
        /*                                 Sélecteurs                                 */
        /* -------------------------------------------------------------------------- */

        /* --------------------------- Version Vanilla JS --------------------------- */
        // for (var i = 0; i < document.getElementsByTagName('p').length; i++) {
        //     document.getElementsByTagName('p')
        // }
        /* -------------------------------------------------------------------------- */

        /* ----------------------------- Version JQuery ----------------------------- */
        // $('p').css('border', "3px solid red")
        /* -------------------------------------------------------------------------- */


        /* -------------------------------------------------------------------------- */


        /* --------------------------- Version Vanilla JS --------------------------- */
        // document.getElementById('list1').style.border = "3px solid red";
        /* -------------------------------------------------------------------------- */

        /* ----------------------------- Version JQuery ----------------------------- */
        // $('#list1').css('border', "3px solid red");
        /* -------------------------------------------------------------------------- */

        /* -------------------------------------------------------------------------- */


        /* --------------------------- Version Vanilla JS --------------------------- */
        // for (var i = 0; i < document.getElementsByTagName('b').length; i++) {

        //     document.getElementsByClassName('b')
        // }
        /* -------------------------------------------------------------------------- */

        /* ----------------------------- Version JQuery ----------------------------- */
        // $(".b").css('border', "3px solid red");
        /* -------------------------------------------------------------------------- */


        /* -------------------------------------------------------------------------- */


        /* --------------------------- Version Vanilla JS --------------------------- */
        // for (var i = 0; i < document.getElementsByTagName('p').length; i++) {
        //     if (document.getElementsByTagName("p")[i].classList.contains("b")) {
        //         document.getElementsByTagName("p")[i].style.border("border", "30px solid red")
        //     }
        // }

        /* ------------------------- Altertanive Vanilla Js ------------------------- */
        /* ---------------------------- querySelector('') --------------------------- */

        //document.querySelector('p.b').style.border = "3px solid blue" 
        // // Il va selectionner le 1er qu'il trouve

        /* -------------------------- querySelectorAll('') -------------------------- */

        //document.querySelectorAll('p.b') 
        // // Il va faire un array contenant tous les p.b, donc il va falloir boucler.

        // for (var i = 0; i < document.querySelectorAll('p.b').length; i++) {
        //     document.querySelectorAll('p.b')[i].style.border = "3px solid blue"
        // }
        /* -------------------------------------------------------------------------- */

        /* ----------------------------- Version JQuery ----------------------------- */
        // $('p.b').css('border', '3px solid red')
        /* -------------------------------------------------------------------------- */



        /* -------------------------------------------------------------------------- */
        /*                                   filtre                                   */
        /* -------------------------------------------------------------------------- */

        /* --------------------------------- basique -------------------------------- */

        /* --------------------------------- :first --------------------------------- */
        /* -------------- Réduisez l'ensemble d'éléments correspondants ------------- */
        /* -------------------- au premier élément de l'ensemble. ------------------- */

        //$('p:first').css('border', "3px solid red"); 
        // //selectionne le premier

        /* -------------------------------------------------------------------------- */

        /* ---------------------------------- :last --------------------------------- */
        /* -------------------- Réduisez l'ensemble des éléments -------------------- */
        /* ------------ correspondants au dernier élément de l'ensemble. ------------ */

        //$('p:last').css('border', "3px solid red"); 
        // //selectionne le dernier

        /* -------------------------------------------------------------------------- */

        /* ---------------------------------- :odd ---------------------------------- */
        /* ------------ Sélectionne les éléments impairs, indexés à zéro ------------ */

        //$('p:odd').css('border', "3px solid red"); 
        // //selectionne les impaires

        /* -------------------------------------------------------------------------- */

        /* --------------------------------- :event --------------------------------- */
        /* ------------ Sélectionne les éléments pairs, indexés à zéro ------------ */

        // $('p:even').css('border', "3px solid blue"); 
        // //selectionne les impaires
        // //!!! l'index commence à 0

        /* -------------------------------------------------------------------------- */

        /* ----------------------------------- :lt ---------------------------------- */
        /* ------------------- Sélectionnez tous les éléments dont ------------------ */
        /* -------- l’indice est inférieur à celui index du jeu correspondant ------- */

        //$('p:lt(2)').css('border', "3px solid red") 
        // // less than selectionne les p  qui ont un index plus petits que 2

        /* -------------------------------------------------------------------------- */

        /* ----------------------------------- :gt ---------------------------------- */
        /* ------------------ sélectionnez tous les éléments dont l ----------------- */
        /* --------- 'index est supérieur à celui indexdu jeu correspondant. -------- */

        // $('p:gt(2)').css('border', "3px solid blue") 
        // // greater than selectionne les p  qui ont un index plus grand que 2

        /* -------------------------------------------------------------------------- */


        /* ----------------------------------- :ep ---------------------------------- */
        /* --------------------- réduit l'ensemble des éléments --------------------- */
        /* --------------- correspondants à celui de l'index spécifié. -------------- */

        // $('p:eq(2)').css('border', "3px solid fuchsia") 
        // // equal selectionne les p qui ont un index égale à 2

        /* -------------------------------------------------------------------------- */

        /* ---------------------------------- :not ---------------------------------- */
        /* ---- Supprimer des éléments de l'ensemble des éléments correspondants. --- */

        //$('p:not(:eq(2))').css('border', "3px solid red") 
        // // tout sauf celui égale à 2

        /* -------------------------------------------------------------------------- */



        /* -------------------------------------------------------------------------- */
        /*                                  Attributs                                 */
        /* -------------------------------------------------------------------------- */


        /* --------------------------------- [class] -------------------------------- */

        //$('p[class]').css('border', "3px solid red"); 
        // //selectionne les p qui ont l'attribut class

        /* -------------------------------------------------------------------------- */

        /* --------------------------------- [title] -------------------------------- */

        //$('p[title]').css('border', "3px solid red"); 
        // //selectionne les p qui ont l'attribut title

        /* -------------------------------------------------------------------------- */

        /* ------------------------------ [title] = ... ----------------------------- */

        //$('p[title = "C\'est le jour J"]').css('border', '3px solid red') 
        // // comence par 

        /* -------------------------------------------------------------------------- */


        /* ----------------------------- [title] ^= ... ----------------------------- */

        //$('p[title^="Bonjour"]').css('border', "3px solid green"); 
        // // fini par

        /* -------------------------------------------------------------------------- */

        /* ----------------------------- [title] $= ... ----------------------------- */

        //$('p[title$="J"]').css('border', "3px solid yellow"); 
        // // se termine par j
        /* -------------------------------------------------------------------------- */


        /* ----------------------------- [title] *= ... ----------------------------- */

        //$('p[title*="jour"]').css('border', "3px solid fuchsia"); 
        // // contient les caractère "jour"

        /* -------------------------------------------------------------------------- */

        /* ----------------------------- [title] ~= ... ----------------------------- */

        //$('p[title~="jour"]').css('border', "3px solid blue"); 
        // // contient le mot "jour" (donc il faut un espace devant et derrière)

        /* -------------------------------------------------------------------------- */


        /* -------------------------------------------------------------------------- */
        /*                                   Contenu                                  */
        /* -------------------------------------------------------------------------- */

        /* -------------------------------- :contains ------------------------------- */

        //$('li:contains("Accueil")').css('border', "3px solid blue");
        // // les li qui contiennent "Accueil"
        /* -------------------------------------------------------------------------- */

        /* --------------------------------- :empty --------------------------------- */

        //$('li:empty').css('border', "3px solid fuchsia");
        // // selectionne les éléments vide 

        /* -------------------------------------------------------------------------- */

        /* -------------------------------- .empty() -------------------------------- */

        //$('li').empty() 
        // // vide le contenu de tous les li

        /* -------------------------------------------------------------------------- */


        /* --------------------------------- :parent -------------------------------- */

        //$(':parent').css('border', "3px solid fuchsia"); 
        //selectionner  les parents de l'élément

        /* -------------------------------------------------------------------------- */

        /* --------------------------------- :has(el) --------------------------------- */

        //$("p:has(span)").css('border', "3px solid red");
        // // réduire l'ensemble des éléments correspondants à ceux dont le descendant 
        // // correspond au sélecteur ou à l'élément DOM.

        /* -------------------------------------------------------------------------- */
        /*                                 Visibilité                                 */
        /* -------------------------------------------------------------------------- */

        /* -------------------------------- :visible -------------------------------- */

        //$("p:visible").css('border', "3px solid red");
        // // Sélectionne tous les éléments visibles.

        /* -------------------------------------------------------------------------- */

        /* --------------------------------- :hidden -------------------------------- */

        //$("p:hidden").css('display', "block");
        // // Sélectionne tous les éléments cachés.

        /* -------------------------------------------------------------------------- */


        /* -------------------------------------------------------------------------- */
        /*                                  Children                                  */
        /* -------------------------------------------------------------------------- */

        /* ------------------------------ :nth-child(i) ----------------------------- */

        //$("p:nth-child(2)").css('border', "3px solid green");
        // // le 2e enfant de la div

        //$("div :nth-child(3)").css('border', "3px solid yellow"); 
        // // le 3e enfant de la div

        /* -------------------------------------------------------------------------- */


        /* ---------------------------- :nth-child(2) el ---------------------------- */

        //$("div :nth-child(2) span").css('border', "3px solid red"); 
        // // le span du 2e enfant de la div

        /* -------------------------------------------------------------------------- */


        /* ---------------------------- :nth-child(2n+1) ---------------------------- */

        //$("p:nth-child(2n+1)").css('border', "3px solid fuchsia"); 
        // //impaire index

        /* -------------------------------------------------------------------------- */


        /* ---------------------------- p:nth-child(odd) ---------------------------- */

        //$("p:nth-child(odd)").css('border', "3px solid fuchsia"); 
        // //impaire index

        /* -------------------------------------------------------------------------- */


        /* ---------------------------- p:nth-child(even) ---------------------------- */

        //$("p:nth-child(even)").css('border', "3px solid fuchsia"); 
        // //paire index

        /* -------------------------------------------------------------------------- */


        /* ------------------------------- :only-child ------------------------------ */

        //$("div :only-child").css('border', "3px solid fuchsia"); 
        // //Sélectionne tous les éléments qui sont le seul enfant de leur parent

        /* -------------------------------------------------------------------------- */


        // :nth teste site web ??? 

        /* -------------------------------------------------------------------------- */


        /* -------------------------------------------------------------------------- */
        /*                                 formulaire                                 */
        /* -------------------------------------------------------------------------- */

        /* --------------------------------- :input --------------------------------- */
        /* ----------------- Sélectionne tous les éléments d’entrée, ---------------- */
        /* --------------- de zone de texte, de sélection et de bouton -------------- */

        //$(':input').before(':INPUT'); 
        // // selectionne tous les éléments de formulaire même si ce n'est pas une balise <Input>

        /* -------------------------------------------------------------------------- */

        /* --------------------------------- :radio --------------------------------- */
        /* -------------- Sélectionne tous les éléments de type radio. -------------- */

        //$(':radio').before("RADIO");

        /* -------------------------------------------------------------------------- */

        /* -------------------------------- :checkbox ------------------------------- */
        /* ----------- Sélectionne tous les éléments de type case à cocher ---------- */

        //$(':checkbox').before("CHECKBOX");

        /* -------------------------------------------------------------------------- */

        /* ---------------------------------- :text --------------------------------- */
        /* ---------- Sélectionne tous les éléments en entrée de type text. --------- */

        //$(':text').before("TEXT");

        /* -------------------------------------------------------------------------- */

        /* -------------------------------- :checked -------------------------------- */
        /* ---------- Correspond à tous les éléments cochés ou sélectionnés --------- */

        //$(':checked').before("CHECKED");

        /* -------------------------------------------------------------------------- */

        /* -------------------------------- :disable -------------------------------- */
        /* ---------------- Sélectionne tous les éléments désactivés ---------------- */

        //$(':disabled').before('DISABLED');

        /* -------------------------------------------------------------------------- */

        /* --------------------------------- :submit -------------------------------- */
        /* -------------- Sélectionne tous les éléments de type submit. ------------- */

        //$(':submit').before('SUBMIT');

        /* -------------------------------------------------------------------------- */







    })
})(jQuery) // Surtout ne pas oublier de l'oublier 